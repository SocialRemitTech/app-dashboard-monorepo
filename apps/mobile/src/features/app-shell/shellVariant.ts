// apps/mobile/src/features/app-shell/shellVariant.ts
/**
 * Presentation A/B for the app shell — flip to demo both to stakeholders, like the welcome variants.
 *   'classic' → original tab bar + simple Home (Variant A)
 *   'hero'    → floating pill tab bar + hero-card Home (Variant B, the new concept)
 */
export const SHELL_VARIANT: 'classic' | 'hero' = 'classic';
