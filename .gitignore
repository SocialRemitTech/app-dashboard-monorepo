node_modules
.expo
.expo-shared
dist
build
.turbo

# Generated native projects — EAS prebuilds these on its servers.
# The leading slash pins these to the REPO ROOT, where a stray `expo prebuild`
# creates them and makes EAS treat the root as the app.
/ios
/android
apps/mobile/ios
apps/mobile/android

# secrets
apps/mobile/google-play-service-account.json
*.keystore
*.p8
*.p12
.env
.env.local

*.log
.DS_Store