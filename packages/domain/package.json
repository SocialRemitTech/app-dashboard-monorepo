{
  "name": "@sr/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": { "@sr/api-contract": "workspace:*" },
  "devDependencies": {
    "@sr/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
